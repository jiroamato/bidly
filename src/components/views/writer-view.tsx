"use client";

export function WriterView({ agent }: { agent: any }) {
  return (
    <div className="p-10">
      <h1 style={{ fontFamily: "'DM Serif Display', serif", fontSize: 28 }}>
        Bid Workspace
      </h1>
      <p className="mt-2 text-sm" style={{ color: "var(--text-muted)" }}>
        Writer agent view — coming soon
      </p>
    </div>
  );
}
